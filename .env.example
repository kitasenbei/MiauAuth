# Auth Service Configuration

# Frontend
FRONTEND_URL=http://localhost
FRONTEND_PORT=5173

# osu! OAuth
OSU_CLIENT_ID=your_osu_client_id
OSU_CLIENT_SECRET=your_osu_client_secret
OSU_REDIRECT_URI=https://your-domain.com/auth/callback

# JWT Secret (MUST BE THE SAME AS MAIN BACKEND)
SECRET_KEY=generate-a-secure-random-key
JWT_ALGORITHM=HS256
JWT_EXPIRATION_DAYS=7

# Main Backend URL (for user creation)
MAIN_BACKEND_URL=http://localhost:8000

# Internal service authentication (MUST BE THE SAME AS MAIN BACKEND)
INTERNAL_SECRET=generate-another-secure-random-key

# Debug
DEBUG=True
